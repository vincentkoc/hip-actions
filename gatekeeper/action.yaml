name: 'Gatekeeper'
description: 'hipages CI gatekeeper'
author: 'https://github.com/viniciusps'
inputs:
  token:
    description: GitHub token
    default: ${{ github.token }}
    required: false
runs:
  using: 'node24'
  main: 'dist/index.js'
branding:
  icon: 'plus-circle'
  color: 'green'
